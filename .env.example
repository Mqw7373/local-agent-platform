# Optional data root. Defaults to %USERPROFILE%\.local-agent-platform on Windows.
LOCAL_AGENT_PLATFORM_HOME=

# Required only when a Reviewer returns ESCALATE and the DeepSeek Challenger runs.
OPENROUTER_API_KEY=

# Optional Multica integration.
MULTICA_WORKSPACE_ID=
MULTICA_TOKEN=
MULTICA_PROJECT_ID=
